export enum TradingEvents {
  Propose = 'propose',
  AcceptProposal = 'accept-proposal',
  DenyProposal = 'deny-proposal',
  Cancel = 'cancel',
  AddItem = 'add-item',
  Lock = 'lock',
  Confirm = 'confirm',
}
